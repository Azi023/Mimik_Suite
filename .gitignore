# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
*.p12
credentials*
*.secret
.keys

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
.turbo/

# Data / runtime artifacts (may contain client PII)
runs/
outputs/
*.sqlite
*.db
uploads/
storage/
artifacts/
var/

# Google service-account keys (Drive archive) — the JSON IS a credential.
# RESTORED 2026-07-22: an earlier uncommitted edit stripped these; a real
# service-account key lives in `secure repo/` — never let it reach git.
secure repo/
secure_repo/
*service-account*.json
*service_account*.json
gen-lang-client-*.json
gcp-*.json

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code — local agent state (per-machine, not shared)
.claude/agent-memory-local/
.claude/settings.local.json
.claude/*.lock
_archive/

